// src/components/Header.js
import React from 'react';

const Header = () => {
  return (
    <header>
      <h1>Expense Tracker</h1>
      {/* You can add additional elements like navigation links */}
    </header>
  );
};

export default Header;
