// Importing the AppRouter.jsx file  to route the page
import AppRouter from './AppRouter';

// defining app component as funtion

const App = () => {
  return (  // return to the JSX file to reder the component needed
    <div>
      <AppRouter />
    </div>
  );
};

export default App; // exporting the file

